exit
params
exit
exidt
params
exit
params
exit
params
exit
params
exit
ids
exit
ids.each do |id|exit
ids
    self.notes << note
    note = Note.find(id)
  ids.each do |id|
  end
    self.notes << note
    note = Note.find(id)
  ids.each do |id|
   28:   end
   27:     self.notes << note
   26:     note = Note.find(id)
ids.each do |id|
 ids.each do |id|
<%= f.collection_select :category_name, Category.all, :name, :name %><%= f.collection_select :category_name, Category.all, :name, :name %>
params
ids
exit
params
